/*
 Prototypes and include guards for project are below
 */

#include <stdio.h>
#include <stdlib.h>

void print(char** grid);

char finalplay(char** grid, int a, int b);

char** makegrid();

int lines(char** grid);
